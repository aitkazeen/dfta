import { ComingSoon } from "../../src/components";

/** Профиль / Ещё (4.8) — ещё не спроектирован. */
export default function MoreScreen() {
  return (
    <ComingSoon
      title="Ещё"
      note="Профиль и настройки появятся на следующем этапе (§4.8)."
    />
  );
}
