import { ComingSoon } from '../../src/components'

/** Лента новостей (4.5) — ещё не спроектирована. */
export default function NewsScreen() {
  return <ComingSoon title="Новости" note="Лента новостей появится на следующем этапе (§4.5)." />
}
