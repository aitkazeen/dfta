import { ComingSoon } from '../../src/components'

/** Алерты (4.6) — ещё не спроектированы. */
export default function AlertsScreen() {
  return <ComingSoon title="Алерты" note="Правила алертов появятся на следующем этапе (§4.6)." />
}
