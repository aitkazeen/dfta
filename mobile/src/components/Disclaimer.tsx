import { useTheme } from "../theme";
import { Text } from "./Text";

/**
 * Единые юридические формулировки (бриф §7). Не менять по месту:
 * текст согласован и обязателен на каждом экране прогноза.
 */
export const LEGAL_DISCLAIMER_FULL =
  "Информационный сервис. Не является индивидуальной инвестиционной рекомендацией. Прошлые результаты не гарантируют будущих.";

export const LEGAL_DISCLAIMER_SHORT =
  "Информационный сервис. Не является индивидуальной инвестиционной рекомендацией.";

/** Раскрытие использования ИИ + согласие на передачу данных третьим сторонам
 *  (App Store / Google Play, правила 2026 — см. roadmap §8). Показывается на
 *  онбординге, согласие фиксируется явным нажатием кнопки. */
export const LEGAL_AI_DISCLOSURE =
  "Пояснения к прогнозам генерирует искусственный интеллект по уже рассчитанным числам — сам прогноз считается детерминированными правилами, не ИИ. Для работы сервиса данные (котировки и новостной фон) обрабатываются сторонними сервисами, включая AI-провайдера. Продолжая, вы соглашаетесь с этим.";

/** Честная точность (roadmap §8): показываем реальную статистику попаданий из
 *  истории прогнозов, а не маркетинговое число. */
export const LEGAL_ACCURACY =
  "Точность прогнозов мы показываем реальную — по фактическим исходам прошлых прогнозов, а не рекламную.";

type Props = {
  /** Короткая формулировка (для подвала экрана) вместо полной. */
  short?: boolean;
  center?: boolean;
};

export function Disclaimer({ short, center }: Props) {
  const { colors } = useTheme();
  return (
    <Text variant="caption" color={colors.textTertiary} center={center}>
      {short ? LEGAL_DISCLAIMER_SHORT : LEGAL_DISCLAIMER_FULL}
    </Text>
  );
}
