# dfta — KursWise

Мобильный финансовый помощник. Ядро продукта: технический анализ графиков валютных пар \+ анализ новостного фона → прогноз направления → push-уведомление.

Владелец разбирается с Docker и бэкендом впервые — **объясняй решения, а не только выдавай код**.

Полная архитектура и план работ: @docs/architecture-roadmap.md

## Скоуп

- **Активы MVP:** USD, EUR, RUB, KZT. **KZT — центральная валюта продукта**, все пары котируются против неё: USD/KZT, EUR/KZT, RUB/KZT. Пар вида USD/RUB или EUR/USD (без KZT) в скоупе нет — RUB и другие валюты интересны продукту только через призму тенге. Список расширяемый, но только новыми парами X/KZT *(изменено 2026-08-07, было: 5 пар включая USD/RUB и EUR/USD)*  
- **Платформы:** iOS \+ Android одновременно  
- **Вне скоупа:** iOS-виджеты (WidgetKit), Live Activities, торговля и брокерские операции, рекомендации по акциям и облигациям, портфельные советы

## Стек

- **mobile/** — Expo (React Native), TypeScript. Запускается локально, не в Docker  
- **server/** — Fastify \+ Prisma, TypeScript, ESM. Живёт в контейнере  
- **БД** — Postgres 16 в контейнере  
- Оркестрация — Docker Compose: `compose.yaml` (база) \+ `compose.override.yaml` (локальные надстройки, подхватывается автоматически)

Появится по мере этапов, но ещё не подключено: TimescaleDB (расширение к той же Postgres, для свечей), Redis \+ BullMQ (кэш и фоновые задачи), TanStack Query \+ Zustand \+ MMKV на клиенте, Victory Native XL для графиков.

## Принятые решения

- **Expo, а не bare React Native** — чтобы не настраивать Xcode/Android Studio на старте  
- **Fastify, а не NestJS** — владелец первый раз делает бэкенд, скелет уже работает. Nest даёт DI и структуру, но добавляет три абстракции поверх той, которую ещё не пощупали. Возврат к вопросу — когда роутов станет больше и появится реальная боль от ручной сборки зависимостей  
- **Prisma, а не Drizzle** — больше документации, порог входа ниже  
- ~~**`prisma db push`, а не миграции**~~ — сделан переход на `prisma migrate dev`/`migrate deploy` 2026-08-09 (первая миграция `candle_hypertable_pk` заодно завела и `CREATE EXTENSION timescaledb` в историю миграций — раньше расширение было накатано вручную и не пережило бы `migrate reset`)  
- **Двухстадийный Dockerfile** — dev-зависимости не уезжают в финальный образ  
- **Прогноз по правилам, а не ML** — фиатные пары с управляемыми курсами (RUB, KZT) плохо предсказываются ML на малой выборке. Правила предсказуемы и отлаживаемы; ML — второй этап, за тем же интерфейсом  
- **LLM только для текста объяснения**, не для расчёта прогноза
- **Gemini, а не Anthropic, — провайдер LLM по умолчанию** (и для объяснения прогноза, и для сентимента RSS-новостей) — решение 2026-08-19. У Google AI Studio есть постоянный бесплатный тариф на Flash-моделях, у Anthropic — только разовый триал-кредит. `ANTHROPIC_API_KEY` остаётся явной платной альтернативой: обе фабрики (`explainer.factory.ts`, `news/sentiment-classifier.factory.ts`) берут Gemini, если задан `GEMINI_API_KEY`, иначе Anthropic, иначе no-op

## Текущее состояние

`Ping`/`/ping` удалены. Первая реальная вертикаль работает целиком: модели `Currency`/`CurrencyPair`/`Candle` (`Candle` пока без данных — пишущего пайплайна ещё нет), `IQuoteProvider` за фабрикой (`server/src/modules/market/`, primary НБ РК + fallback ForexRateAPI, переключение при отказе), сид трёх одобренных пар и роут `GET /v1/pairs/:id/quote`. Проверено вживую 2026-08-08: `docker compose up --build` → `npm run db:seed` → все 3 пары отдают текущий курс от НБ РК, `GBP-KZT` (не сидированная пара, вне скоупа) отдаёт 404. Также проверено, что `npm run build` (tsc) + `node dist/main.js` — путь продакшен-стадии Dockerfile — не падает на резолве относительных импортов (нужны explicit `.js`-расширения при module: ES2022 + moduleResolution: bundler).

BullMQ-воркер (`server/src/worker.ts`, отдельный контейнер `worker` в `compose.yaml`/`compose.override.yaml`) каждые 6 часов обходит активные пары через тот же `IQuoteProvider` и складывает дневные свечи в `candle` (upsert по `(pairId, timeframe, ts)` — open фиксируется первым снятием за день, high/low/close обновляются на каждом). 6 часов, а не чаще: НБ РК отдаёт один фиксинг в сутки, курс внутри дня не меняется. Проверено вживую 2026-08-09: `docker compose up --build` → воркер стартует, забирает курсы всех 3 пар от НБ РК и пишет 3 строки в `candle`.

`candle` теперь настоящая TimescaleDB hypertable (композитный PK `(pairId, timeframe, ts)` вместо отдельного `id`, миграция `candle_hypertable_pk`), схема перешла с `db push` на `prisma migrate dev`/`migrate deploy` — проверено вживую 2026-08-09.

Мобильное приложение: навигация и дизайн-система уже были готовы (Expo Router, `app/(tabs)` + `app/pairs/[id]`, тема, компоненты) — это выяснилось при разборе задачи, ошибочно значилось «впереди» в более раннем состоянии этого файла. Watchlist и экран пары (`app/(tabs)/index.tsx`, `app/pairs/[id].tsx`) теперь берут курс/дельту/свечи с бэкенда (`GET /v1/pairs`, `/quote`, `/candles` — свежедобавленные роуты) вместо `mobile/src/mock/*`; `GBP-KZT`/`CNY-KZT` убраны из watchlist как не входящие в скоуп. forecast/indicators/news/accuracy на экране пары остаются моком — бэкенд их не считает (этапы 3–4). Новости и алерты (`(tabs)/news.tsx`, `alerts.tsx`, `more.tsx`) — намеренные заглушки `ComingSoon`, ещё не спроектированы.

Проверено вживую 2026-08-09: `docker compose up --build` + `/v1/pairs`, `/v1/pairs/:id/quote`, `/v1/pairs/:id/candles` отдают реальные данные; `npx tsc --noEmit` чист в `server/` и `mobile/`; `npx expo start --web` собирает бандл без ошибок (визуальную проверку на реальном экране ещё не проводили — нет доступа к браузеру/симулятору в этой сессии). Новостной модуль и forecast-движок — ещё впереди по этапу 1.

**Этот файл на тот момент не обновлялся вслед за кодом** — с 2026-08-09 без записи сюда добавились: полный расчёт индикаторов (`server/src/modules/indicators/`, EMA20/50/200, RSI14, MACD, Stochastic, ATR14, ширина Боллинджера), `RulesForecastEngine` (`server/src/modules/forecast/`) — воркер раз в сутки на пару пишет `forecast` и резолвит `forecast_outcome` по истечении горизонта, роут `GET /v1/pairs/:id/forecast`, и CI (`.github/workflows/ci.yml`, lint+test+build для server и mobile). На экране пары в приложении direction/confidence/target-диапазон/индикаторы уже реальные с бэкенда; news/accuracy остаются моком.

**2026-08-16 — LLM-объяснение прогноза (правило 3).** `Explainer` за фабрикой (`server/src/modules/forecast/explainer.factory.ts`, `AnthropicExplainer` + `CachingExplainer`-декоратор поверх Redis по хэшу входа, TTL 30 дней) — воркер зовёт его в `maybeGenerateForecast` после расчёта `RulesForecastEngine` и пишет `explanation`/`drivers` в `forecast`; без `ANTHROPIC_API_KEY` фабрика отдаёт no-op-реализацию, ничего не падает. Мобильный `ForecastCard` уже был готов принимать эти поля (`mapForecast` в `mobile/src/lib/forecast.ts` ждал их с самого начала). Промпту явно запрещено придумывать новости/решения регулятора, которых нет во входных данных. 17 unit-тестов (кэш, парсинг ответа модели, ретраи, no-op-фабрика) зелёные, `tsc --noEmit` и `npm run build` в `server/` чисты. *(Обновлено 2026-08-19: фабрика теперь Gemini-first, см. «Принятые решения»; модель по умолчанию — `gemini-flash-latest`, не `claude-haiku-4-5-20251001`.)* Проверено вживую 2026-08-23: `docker compose up --build`, реальный `GEMINI_API_KEY` в корневом `.env`, воркер генерирует `explanation`/`drivers` без ошибок.

**2026-08-19/20/23 — новостной модуль (этап 3 роадмапа).** `server/src/modules/news/`: `INewsSource` за фабрикой (`sources.factory.ts`) — `NbkRssSource` (`nationalbank.kz/rss_news_russian.xml`, обязателен, без ключа) + `MarketauxSource` (нужен `MARKETAUXAPI_KEY`, опционален, source отдаёт entities с валютными тегами напрямую). `SentimentClassifier` за фабрикой (`sentiment-classifier.factory.ts`, тот же Gemini/Anthropic/no-op выбор, что у explainer) классифицирует батчем RSS-статьи без готового сентимента; у Marketaux сентимент уже готов, LLM не зовётся. Пайплайн (`pipeline.ts`) — дедуп дублей между источниками (`dedup.ts`, сравнение заголовков + временное окно, канонической считается статья с более высоким `sourceWeight`), фильтр релевантности по паре (Marketaux — по entities, RSS — по ключевым словам `relevanceKeywords` в `config.ts`), `impactScore = sourceWeight × freshness(halfLife 12ч)`, запись в `news_article`/`news_pair_link`. `getNewsScore` (`repository.ts`) — взвешенное среднее `our_sentiment`/`raw_sentiment` за 24ч с весом `impactScore`, 0 если новостей за окно не было. Воркер (`collectNews` в `worker.ts`) гоняет пайплайн каждые 15 минут, отдельный от опроса котировок (6ч) BullMQ job scheduler `collect-news`. `RulesForecastEngine` теперь получает `newsScore` по-настоящему (`worker.ts:maybeGenerateForecast` зовёт `getNewsScore` перед `predict()`) — слияние `technicalScore * 0.6 + newsScore * 0.4` (roadmap §5.3) больше не теоретическое, а вычисляется на реальных данных.

**2026-08-23 — два живых бага найдены и исправлены при проверке новостного пайплайна.** (1) `GEMINI_API_KEY` в корневом `.env` оказался токеном не того типа (формат `AQ.Ab8...`, не `AIzaSy...` — AI Studio отвечал `401 ACCESS_TOKEN_TYPE_UNSUPPORTED`); из-за no-op-дизайна фабрик это не роняло пайплайн, а тихо резало RSS-статьи без сентимента (`pipeline.ts`: `if (sentiment === undefined) continue` — статья не пишется вообще, не просто получает нейтральный скор) и объяснения прогнозов. Ключ заменён, проверено вживую прямым вызовом Gemini API и живым прогоном воркера. (2) НБ РК отдаёт `rss_news_russian.xml`, но описания в нём фактически на казахском — `relevanceKeywords` в `news/config.ts` были только на русском/английском, поэтому все 10 статей RSS в тестовом прогоне отсеивались до сентимент-классификации. Добавлено казахское `еуро` (доллар/рубль/теңге совпадают в написании с русским) — не полное решение (казахоязычные RSS-новости на другие темы вообще могут не содержать релевантных ключевых слов), но закрывает конкретно найденный разрыв.

**2026-08-23 — калибровка confidence (правило 6, этап 4 роадмапа).** `server/scripts/backtest-forecast.ts` — walk-forward бэктест `RulesForecastEngine` по 2-летнему датасету НБ РК, уже забэкфилленному в `candle` (`npm run fx:backfill-dataset`, 738 дневных свечей × 3 пары). Идёт день за днём по каждой паре, вызывает `predict()` с indicators, посчитанными только по свечам до текущего дня (честный walk-forward, без заглядывания вперёд), сверяет direction с `resolveForecastOutcome` на факте через 24ч/7д. **Важное упрощение:** `newsScore = 0` для всей истории — двухлетнего архива новостей не существует (новостной пайплайн запущен только в 2026-08), калибровка отражает фактически только technicalScore-часть блендинга. Результат бинуется по децилям сырого `|blendedScore|`, прогоняется через isotonic regression (pool-adjacent-violators — гарантирует, что confidence не падает с ростом скора) → `server/src/modules/forecast/calibration.ts` (генерируемый файл, `npm run forecast:backtest` перезаписывает). `RulesForecastEngine.predict()` теперь берёт `confidence` из `calibrateConfidence(rawScore, horizon)`, не из `Math.abs(blendedScore)` напрямую.

**Находка бэктеста — точность прогноза близка к случайной.** Для горизонта 24ч реальный hit rate по всем бакетам скора — 27–40% (ниже, чем ~33% у случайного угадывания между up/down/flat), калиброванная confidence не превышает ~0.35. Для 7д лучше (50–54% при score ≥0.1), но нижний бакет (score < 0.1) даёт всего 7.8%. Это эмпирическое подтверждение риска, который роадмап называл гипотетическим (§10: «Точность прогноза окажется около случайной — Высокая [вероятность]»). *(Расследовано и уточнено 2026-08-23, см. следующую запись — «7.8%» оказался артефактом фиксированной мёртвой зоны, а сам вывод об отсутствии edge подтвердился и усилился.)*

**2026-08-23 — расследование точности + подбор весов (закрытие этапа 4, roadmap §9).** Три находки, все проверены вживую на воспроизведённом baseline (Postgres в Docker, 731 дневная свеча × 3 пары НБ РК, `npm run fx:backfill-dataset` → `forecast:backtest`):

1. **Провал 7д-бакета [0,0.1) = 7.8% был багом определения «flat», а не сигналом.** Факт считался `flat` только при движении < 0.1% (`deadZonePct = flatThreshold*maxMovePct = 0.001`), а за 7 дней курс почти всегда двигается больше — поэтому все прогнозы-`flat` засчитывались промахом. Исправлено: мёртвая зона факта теперь волатильностно- и горизонт-относительная — `flatBand = flatBandAtrMult × atr14 × sqrt(horizonDays)`, `flatBandAtrMult = 0.5` (`config.ts`). `resolveForecastOutcome` принимает абсолютный `flatBand` вместо `deadZonePct` (`outcomes.ts`); worker и бэктест считают его одинаково.
2. **Тихий NaN на single-fix свечах.** В периоды пега `ATR14 = 0` и `MACD = 0` → `macd/atr14 = 0/0 = NaN` протекал через `clamp`/`weightedAverage` и портил весь `technicalScore`. Закрыто: `atrRelative` возвращает `null` при `atr <= 0`, `weightedAverage` фильтрует по `Number.isFinite` (`compute.ts`).
3. **Главный вывод: направленного edge нет ни у одной конфигурации.** Новый `scripts/tune-weights.ts` (`npm run forecast:tune`) — walk-forward grid-search по всем миксам весов (trend/momentum × составы momentum-сигналов) × порогам × прямому/инвертированному сигналу, метрика — **directional precision** (среди дней, где модель взяла направление, доля совпадений с фактом) против **наивного baseline** «всегда предсказывай более частое направление» (~52%, т.к. факт-баланс 7д ≈ up 34% / down 37% / flat 29%). Лучший edge среди ВСЕХ конфигов = **−9.9%** (7д, trend-heavy), среди инвертированных (mean-reversion) = **−16.6%**. То есть технический сигнал на дневных фиксингах НБ РК направленного преимущества не даёт, и это не ошибка знака. Веса в `config.ts` выставлены **не ради точности (её нет), а по принципу**: `trend 0.7 / momentum 0.3` — тренд несёт ту структуру, что вообще есть; momentum-индикаторы на single-fix свечах вырождены. Итоговая калибровка (перегенерирована): confidence честно упирается в **0.34 (24ч) / 0.42 (7д)** — никаких фиктивных «68%». 37 unit-тестов зелёные, tsc/lint/prettier чисты.

**Продуктовый вывод (не код):** ценность продукта — не в точности прогноза (её эмпирически нет на этих данных), а в агрегации + объяснении + уведомлениях (ровно как митигация в roadmap §10). Реальные пути поднять сигнал требуют новых данных, а не подбора весов: (а) intraday-котировки вместо одной дневной фиксации НБ РК (тогда ATR/Stochastic перестанут быть вырожденными); (б) исторический архив новостей, чтобы `newsScore` участвовал в калибровке (сейчас = 0 на всей истории); (в) макро-фичи (даты заседаний ЦБ). До этого — показывать честную точность из `forecast_outcome`, не продавать её как фичу.

**2026-08-26 — подбор весов сделан, результат отрицательный (закрывает этап 4).** Два открытых вопроса из записи выше проверены прямым экспериментом, не рассуждением.

*Вопрос про ATR/Stochastic на вырожденном OHLC* — снят. Прямой запрос по всем 2217 дневным свечам подтвердил: 100% строк имеют `open = high = low = close` (НБ РК действительно даёт только одну дневную фиксацию). Это не ломает индикаторы, а меняет, что именно они измеряют: True Range при H=L=C вырождается в `|close − prevClose|`, поэтому ATR14 — это среднее дневное изменение цены за 14 дней, вполне осмысленная мера волатильности. Stochastic %K при том же вырождении — это позиция сегодняшнего close внутри диапазона последних 14 close, тоже осмысленно (просто не «внутридневной» Stochastic в классическом смысле). Оба сигнала остаются валидными числами — искажение не является причиной низкой точности.

*Подбор весов* — сделан скриптом `server/scripts/tune-forecast-weights.ts` (`npm run forecast:tune-weights` из `server/`, новый, остаётся в репозитории). Экспортированы нормализующие функции индикаторов (`difference`/`centered100`/`atrRelative`/`weightedAverage` в `forecast/compute.ts`), чтобы скрипт считал те же под-сигналы (trend/rsi/stoch/macd) без дублирования формул. Методология: walk-forward по тем же 2 годам/3 парам, что и `backtest-forecast.ts`, но с train/test split по времени 75/25 на каждую пару — иначе конфиг, подогнанный под шум, было бы не отличить от реально работающего на всего ~2000 пересекающихся (горизонт 7д) наблюдениях.

**Результат:** ни одна из проверенных конфигураций весов (equal, trend-only, rsi-only, stoch-only, macd-only, momentum-only, drop-any-one, trend-heavy) не обгоняет тривиальный бейзлайн «всегда предсказывать самый частый исторический класс (up/down/flat)» — ни на train, ни (за редким и нестабильным исключением) на отложенном test. Пример (24h, test): equal-weights даёt 35.2% точности при бейзлайне 38.3%; лучший найденный вариант — 37.9% (rsi-only), тоже ниже бейзлайна. На 7д ещё нагляднее: класс-распределение сильно смещено (up 45.5% / down 48.7% / flat всего 5.8% — за неделю пара почти всегда куда-то заметно двигается), бейзлайн «всегда down» даёт 56.0% на test, а лучшая техническая конфигурация — 48.4% (macd-only), причём именно macd-only на train проигрывает другому кандидату («drop macd») — ранжирование конфигураций переворачивается между train и test, что само по себе признак подгонки под шум, а не сигнала. Свип `flatThreshold` (порог, отделяющий 'flat' от 'up'/'down') показал тот же паттерн: более консервативный порог на 24h слегка помогает, на 7д сильно вредит — так как порог общий на оба горизонта, выигрыша по сумме нет.

**Решение: веса в `forecastConfig` оставлены как есть, осознанно, а не потому что не пробовали.** Технические индикаторы, посчитанные по единственной дневной фиксации НБ РК, не показывают воспроизводимого направленного преимущества ни при какой линейной перевесовке — дальше подбирать веса на этих же 2 годах/3 парах значит подгонять шум, а не искать сигнал. Это закрывает пункт «подбор весов» из §9 роадмапа отрицательным, но честным результатом (rule «честная точность», §8 роадмапа) — не тем результатом, на который рассчитывали, но полученным экспериментом, а не предположением. Реальный источник потенциального улучшения, если он есть, — не веса, а newsScore (для которого 2-летнего исторического архива нет и калибровка по-прежнему `newsScore = 0`) или дополнительные индикаторы за пределами текущего набора. `calibration-data.ts` перегенерирован тем же прогоном `forecast:backtest` — числа не изменились (сдвиг confidence на ≤0.2пп, за счёт пары новых свечей, накопленных воркером с 08-23).

## Как запускать

docker compose up \--build      \# бэкенд \+ база, из корня

cd mobile && npx expo start    \# приложение, отдельный терминал

Проверка бэкенда: [http://localhost:3000/health](http://localhost:3000/health)

## Что важно помнить

- **`DATABASE_URL` использует хост `db`**, не `localhost` — это имя сервиса в сети Compose. В `.env.example` для запуска без Docker стоит `localhost` — так и задумано  
- **Fastify слушает `0.0.0.0`** — обязательно внутри контейнера  
- **Адрес бэкенда в приложении вычисляется автоматически** в `mobile/src/api.ts` из `Constants.expoConfig.hostUri`. Не хардкодь туда IP  
- **npm 12 сломает этот проект** — он отключает postinstall-скрипты, на которых держатся Prisma, esbuild и нативные модули RN. Владелец сидит на npm 10.9 сознательно, не предлагай обновляться без разговора  
- **В `server/` может лежать лишний `node_modules`** — артефакт сборки, на образ не влияет (отсекается `.dockerignore`)  
- **Секреты только в `.env`**, никогда в репозитории. Сейчас там `GEMINI_API_KEY` и `MARKETAUXAPI_KEY` (оба опциональны — фабрики уходят в no-op без ключа, см. «Принятые решения»)

## Архитектурные правила — не нарушать

1. **Все внешние источники — за интерфейсом.** `IQuoteProvider`, `INewsProvider`. Каждый: primary \+ 1–2 fallback, переключение при отказе. Бизнес-логика никогда не зовёт провайдера напрямую и не знает, откуда пришли данные. Без Nest-овского DI это делается фабрикой, выбирающей реализацию по конфигу — просто и явно  
2. **Прогнозный движок — за интерфейсом `ForecastEngine`** (`predict(input): Promise<ForecastResult>`, `readonly version: string`). v1 \= `RulesForecastEngine`. v2 \= `MlForecastEngine` позже. Замена движка не должна трогать приложение  
3. **LLM вызывается ТОЛЬКО для генерации текста объяснения** по уже посчитанным числам. Никогда — для расчёта самого прогноза. Результат должен быть воспроизводимым. Кэш по хэшу входа  
4. **Веса индикаторов и пороги — в конфиге, не в коде**  
5. **`forecast_outcome` пишется всегда.** Каждый прогноз по истечении горизонта резолвится фактом. Без этого нельзя ни показать честную точность, ни сравнить движки  
6. **`confidence` берётся из калиброванной таблицы** «скор → фактическая частота попадания» на walk-forward бэктесте (2+ года), не из формулы наугад  
7. **`/v1/pairs/snapshot` отдаётся из кэша.** Главный экран не делает N запросов по числу пар в watchlist  
8. **Каждый внешний вызов — с таймаутом и ретраем.** Провайдер данных упадёт, это вопрос времени

## Прогнозный пайплайн

- `technicalScore ∈ [−1, +1]`: EMA20/50/200 \+ наклон, RSI(14), MACD(12,26,9), Stochastic, ATR(14), ширина Боллинджера, локальные экстремумы 30/90д. Считается на каждой свече, кэшируется в `indicator_value`  
- `newsScore ∈ [−1, +1]`: дедуп (URL-хэш \+ эмбеддинг заголовка) → фильтр релевантности по сущностям → сентимент (Marketaux где есть, LLM-классификация батчем для локальных RSS) → `impact_score` \= источник × свежесть × релевантность → взвешенное среднее за 24ч с экспоненциальным затуханием  
- Слияние v1: `technicalScore * 0.6 + newsScore * 0.4`, далее калибровка  
- Частота: 1 прогноз в сутки на пару (утро) \+ внеплановый при резком движении или важной новости

## Источники данных

- **Котировки:** **исторический 2-летний датасет для калибровки — целиком из НБ РК** (`nationalbank.kz/rss/get_rates.cfm`) — бесплатен, без ключа, не ограничен глубиной истории. Решение принято 2026-08-08 после живого прогона: ForexRateAPI на free-тире отдаёт историю только за последние ~30 дней (`statusCode 209` на более старых датах), UniRateAPI исторического эндпоинта не имеет вообще. Скрипт: `server/scripts/export-nbk-dataset.ts` → `server/data/nbk-rates-2y.csv` (в `.gitignore`, регенерируется)  
  + платные провайдеры (кандидаты: ForexRateAPI, UniRateAPI, exchangeratesapi.io, Twelve Data) остаются на роль **live/intraday-источника** в будущем `IQuoteProvider` — там 30-дневное окно free-тира не мешает. Выбор конкретного live-провайдера ещё не сделан. Frankfurter не годится ни для одной пары MVP (KZT не поддерживает, а EUR/USD без KZT вне скоупа) — см. architecture-roadmap.md §3.1  
- **Новости:** Marketaux (глобальный фон, готовый sentiment) \+ собственный RSS-коллектор: ЦБ РФ, НБ РК, Минфин, Interfax, KASE. Проверено вживую: `countries=kz` у Marketaux даёт 0 результатов — казахстанских источников там нет, RSS-коллектор НБ РК/KASE обязателен, не опционален (детали — architecture-roadmap.md §3.2)  
- **Запрещённые источники:** Investing.com (нет публичного API, T\&C прямо запрещают использование их данных в сервисах) и TradingView (только iframe-виджеты, непригодны для RN). Не предлагать их  
- **Макро-календарь:** для MVP — ручной seed-справочник дат заседаний ЦБ РФ / НБ РК / ФРС / ЕЦБ

## Юридические ограничения — критично

- **Никаких инвестиционных рекомендаций.** Формулировки: «вероятное направление», «аналитический сигнал». Никогда — «покупайте», «продавайте», «стоит войти». Показываем: направление \+ вероятность \+ горизонт \+ 3 причины  
- **Никаких персональных рекомендаций по инструментам** (акции, облигации, портфель). В РФ это инвестиционное консультирование (СРО \+ реестр ЦБ), в РК — лицензия АРРФР. Обезличенная аналитика допустима, персональная — нет  
- **Никаких кнопок исполнения сделок и интеграций с брокером** — иначе Apple требует подачу приложения лицензированной финансовой организацией  
- **Дисклеймеры** на онбординге и на экране прогноза. **Раскрытие использования ИИ** \+ согласие на передачу данных третьим сторонам  
- **Точность показываем реальную**, из `forecast_outcome`, не маркетинговую  
- Privacy Manifest (`PrivacyInfo.xcprivacy`) \+ Google Play Data Safety обязательны

## Уведомления

Push — единственный внешний канал (виджета нет), поэтому с первого релиза обязательны: настройки по типам, тихие часы по TZ пользователя, дедуп и лимит на пару в сутки, `notification_log` \+ метрика open rate. Подробности — §7 роадмапа.

## Конвенции кода

- TypeScript strict везде, ESM  
- Валидация входов и выходов — схемы, общие для клиента и сервера  
- Именование в БД — `snake_case`, в коде — `camelCase`  
- Скелет без бизнес-логики — намеренно, не спеши его заполнять

## Документация в репозитории

- `README.md` — команды на каждый день  
- `GUIDE.md` — подробный разбор: что каждый файл делает, путь запроса от кнопки до Postgres, типичные ошибки. Если объясняешь устройство проекта — сверяйся с ним, чтобы не противоречить  
- `docs/architecture-roadmap.md` — целевая архитектура, модель данных, план работ по этапам

## Планы

1. Заменить модель `Ping` на реальные сущности (`currency`, `currency_pair`, `candle`) — см. §4 роадмапа  
2. Подключить первый FX-провайдер за `IQuoteProvider`, начать складывать котировки в БД  
3. Разнести роуты по файлам, когда их станет больше пяти  
4. Перейти с `db push` на миграции  
5. Навигация: `expo-router`. Запросы: `@tanstack/react-query`

**Текущий этап:** 4 закрыт, следующий — 5. Этап 1 (фундамент) и этап 2 (данные/графики) закрыты — `IQuoteProvider`, hypertables, миграции, индикаторы, мобильный watchlist/экран пары на реальных данных, CI (lint+test+build). Этап 3 (новости) реализован целиком: `INewsSource`/`SentimentClassifier` за фабриками, NBK RSS + Marketaux, дедуп, релевантность, `newsScore` пишется и реально участвует в `RulesForecastEngine` — см. записи 2026-08-19/20/23 выше. Из этапа 3 не сделан только сам экран ленты новостей в приложении (мобильный `news.tsx` — по-прежнему `ComingSoon`-заглушка, делает владелец руками — вне зависимости от этапов бэкенда). Этап 4 (прогнозный движок) — `RulesForecastEngine`, LLM-объяснение (Gemini), калибровка confidence по walk-forward бэктесту (2026-08-23, правило 6) и подбор весов (2026-08-26, см. запись выше) сделаны. Подбор весов закрыт отрицательным результатом: реальная точность близка к случайной и никакая перевесовка технических сигналов на имеющихся данных (2 года, одна дневная фиксация НБ РК) её не улучшает — это задокументированный вывод, а не пропущенный пункт. Дальше по этапу 4 действовать не на чем, пока не появится историческая новостная лента или новые источники сигнала. Следующий — этап 5 (уведомления); также вне очереди по-прежнему остаются: экран ленты новостей (этап 3) и EAS Build (мобильные сборки для стора). *(Обновлять эту строку при переходе между этапами — она задаёт контекст, что сейчас в работе.)*  
